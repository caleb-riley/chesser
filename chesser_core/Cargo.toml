[package]
name = "chesser_core"
version = "0.1.0"
edition = "2024"

[dependencies]
mlua = { version = "0.11.6", features = ["luajit", "vendored", "send"] }
rand = "0.9.2"
uuid = { version = "1.21.0", features = ["v4"] }
